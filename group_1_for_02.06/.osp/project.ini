; Конфигурация проекта для OpenServer.
; Замените название секции в квадратных скобках на свой домен
; (рекомендуется фамилия в транслите, например [bondarev.local]),
; сохраните файл и перезапустите OpenServer.
;
; Если в вашей версии OpenServer проект не стартует — можно оставить
; только строку с доменом в квадратных скобках, остальные удалить.

[konferencii.local]
public_dir = ''
